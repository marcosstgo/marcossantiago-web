---
import Base from '../../../layouts/Base.astro';
import Nav from '../../../components/Nav.astro';
import Footer from '../../../components/Footer.astro';

const stack = ['WordPress', 'WooCommerce', 'LiveCanvas', 'Mailgun', 'SEO Técnico'];
---
<Base
  title="Caso de estudio: Yanet Jabones Naturales — Marcos Santiago"
  description="Modernización integral de la tienda en línea de una marca puertorriqueña de jabones artesanales — WordPress, WooCommerce y LiveCanvas, 91/100 de rendimiento móvil."
>
  <Nav />
  <main id="main" class="case" style="--c:#4F7A2A; padding-top:120px">
    <div class="container">
      <a href="/portafolio/dev/" class="back-link">← Portafolio</a>

      <div class="case-head">
        <div>
          <p class="case-label">Caso de estudio · Sitio web para cliente</p>
          <h1 class="case-title">Yanet Jabones Naturales</h1>
          <p class="case-tagline">Modernización integral de la tienda en línea de una marca puertorriqueña dedicada a la elaboración de jabones artesanales y productos para el cuidado de la piel.</p>
        </div>
        <div class="case-meta">
          <span class="case-status live">Live</span>
          <span class="case-year">2026</span>
        </div>
      </div>

      <div class="case-tags">
        {stack.map(t => <span class="tag">{t}</span>)}
      </div>

      <img
        src="/portfolio-cases/yanetshop/home-hero.webp"
        alt="Página de inicio de Yanet Jabones Naturales, rediseñada"
        class="case-hero-img"
        width="1200" height="683" loading="eager"
      />

      <div class="case-grid">
        <section class="case-col">
          <h2 class="case-h2">El reto</h2>
          <p>Yanet Jabones Naturales necesitaba modernizar su tienda en línea para que reflejara mejor la calidad, la trayectoria y el carácter artesanal de la marca. Con un catálogo amplio y una operación ya establecida, el nuevo sitio debía ofrecer una experiencia de compra más clara, rápida y coherente, tanto para sus clientes como para la administración diaria del negocio.</p>
          <p>El reto principal era realizar esta transformación mientras la tienda continuaba recibiendo pedidos. El rediseño debía desarrollarse sin interrumpir las ventas y protegiendo cuidadosamente el catálogo, la información de los productos y las órdenes existentes.</p>
        </section>
        <section class="case-col">
          <h2 class="case-h2">El enfoque</h2>
          <p>El proyecto se desarrolló en un entorno independiente utilizando WordPress, WooCommerce y LiveCanvas, lo que permitió reconstruir y probar cada sección de la tienda sin afectar el funcionamiento del sitio anterior mientras seguía recibiendo pedidos.</p>
          <p>La nueva dirección visual parte de la esencia artesanal de los productos, con una paleta inspirada en tonos naturales y terracota, acompañada por una combinación tipográfica cálida y contemporánea. Además, se reorganizó el catálogo y se rediseñaron las páginas principales del proceso de compra: tienda, producto, carrito y checkout.</p>
          <p>También se trabajó la experiencia móvil, la optimización técnica, la estructura SEO y las comunicaciones transaccionales de WooCommerce, incluyendo correos de pedidos adaptados al idioma y a la identidad de la tienda. Una vez todo quedó probado, la migración final a producción se hizo sin perder un solo pedido ni interrumpir las ventas.</p>
        </section>
      </div>

      <div class="case-shots">
        <img src="/portfolio-cases/yanetshop/store.webp" alt="Tienda de Yanet Jabones Naturales con catálogo de productos" loading="lazy" width="1200" height="683" />
        <img src="/portfolio-cases/yanetshop/product.webp" alt="Ficha de producto individual de Yanet Jabones Naturales" loading="lazy" width="1200" height="683" />
      </div>

      <div class="case-grid case-grid-result">
        <section class="case-col">
          <h2 class="case-h2">El resultado</h2>
          <p>El sitio nuevo ya está en producción, reemplazando por completo a la tienda anterior. La migración de base de datos, catálogo, órdenes y clientes se completó sin perder un solo pedido, con una experiencia visual más clara, coherente y completamente adaptable a dispositivos móviles.</p>
          <p>El catálogo quedó reorganizado con 63 productos activos, y el sitio en vivo mantiene un balance saludable entre diseño, funcionalidad y velocidad en las pruebas de rendimiento móvil de Lighthouse.</p>
          <div class="case-stats">
            <div class="case-stat">
              <span class="case-stat-num">0</span>
              <span class="case-stat-label">Pedidos perdidos<br/>en la migración</span>
            </div>
            <div class="case-stat">
              <span class="case-stat-num">87<span class="case-stat-unit">/100</span></span>
              <span class="case-stat-label">Rendimiento móvil<br/>(Lighthouse, en vivo)</span>
            </div>
            <div class="case-stat">
              <span class="case-stat-num">63</span>
              <span class="case-stat-label">Productos activos<br/>en el catálogo</span>
            </div>
          </div>
        </section>
        <img src="/portfolio-cases/yanetshop/home-mobile.webp" alt="Vista móvil de Yanet Jabones Naturales" class="case-mobile-img" loading="lazy" width="600" height="1200" />
      </div>

      <div class="contact-cta">
        <div>
          <h2 class="cta-title">¿Tu tienda en línea necesita evolucionar?</h2>
          <p class="cta-sub">Diseño y desarrollo tiendas en WordPress y WooCommerce pensadas para facilitar las ventas, mejorar la experiencia de compra y simplificar la administración diaria de tu negocio. Cuéntame sobre tu proyecto.</p>
        </div>
        <a href="https://wa.me/17872439670" target="_blank" rel="noopener" class="btn whatsapp">
          <i class="fab fa-whatsapp"></i> (787) 243-9670
        </a>
      </div>
    </div>
  </main>
  <Footer />
</Base>

<style>
.back-link {
  display: inline-block; font-family: var(--mono); font-size: var(--text-2xs);
  letter-spacing: 1.5px; text-transform: uppercase; color: var(--muted);
  margin-bottom: 40px; transition: color .2s;
}
.back-link:hover { color: var(--c) }

.case-head {
  display: flex; justify-content: space-between; align-items: flex-start;
  gap: 24px; flex-wrap: wrap; margin-bottom: 20px;
}
.case-label {
  font-family: var(--mono); font-size: var(--text-2xs);
  letter-spacing: 2px; text-transform: uppercase; color: var(--c); margin-bottom: 10px;
}
.case-title {
  font-family: var(--display); font-size: clamp(2.4rem, 5vw, 4rem);
  letter-spacing: 2px; color: var(--white); margin-bottom: 12px;
}
.case-tagline { color: var(--muted); font-size: var(--text-lg); line-height: 1.6; max-width: 56ch }
.case-meta { display: flex; flex-direction: column; align-items: flex-end; gap: 8px; flex-shrink: 0 }
.case-status {
  font-family: var(--mono); font-size: .68rem; letter-spacing: 1.5px; text-transform: uppercase;
  padding: 3px 10px; border-radius: 20px;
}
.case-status.preview {
  background: color-mix(in srgb, #facc15 10%, transparent);
  border: 1px solid color-mix(in srgb, #facc15 30%, transparent);
  color: #facc15;
}
.case-status.live {
  background: color-mix(in srgb, #4ade80 12%, transparent);
  border: 1px solid color-mix(in srgb, #4ade80 30%, transparent);
  color: #4ade80;
}
.case-year { font-family: var(--mono); font-size: var(--text-2xs); letter-spacing: 2px; color: var(--dim) }

.case-tags { display: flex; flex-wrap: wrap; gap: 6px; margin-bottom: 40px }
.case-tags .tag {
  font-family: var(--mono); font-size: .68rem; letter-spacing: 1.5px; text-transform: uppercase;
  padding: 3px 9px; border-radius: 4px;
  background: color-mix(in srgb, var(--c) 10%, transparent);
  border: 1px solid color-mix(in srgb, var(--c) 30%, transparent); color: var(--c);
}

.case-hero-img {
  width: 100%; height: auto; border-radius: 14px; border: 1px solid var(--border);
  margin-bottom: 56px;
}

.case-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 40px; margin-bottom: 56px }
@media (max-width: 800px) { .case-grid { grid-template-columns: 1fr; gap: 32px } }
.case-h2 {
  font-family: var(--display); font-size: clamp(1.5rem, 2.5vw, 1.9rem);
  letter-spacing: 1.5px; color: var(--white); margin-bottom: 16px;
}
.case-col p { color: var(--muted); font-size: var(--text-base); line-height: 1.75; margin-bottom: 14px }
.case-col strong { color: var(--white) }

.case-shots { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; margin-bottom: 56px }
@media (max-width: 800px) { .case-shots { grid-template-columns: 1fr } }
.case-shots img { width: 100%; height: auto; border-radius: 12px; border: 1px solid var(--border) }

.case-grid-result { align-items: center }
.case-mobile-img {
  width: 100%; max-width: 280px; height: auto; border-radius: 16px;
  border: 1px solid var(--border); margin: 0 auto; display: block;
}
.case-stats { display: flex; gap: 32px; margin-top: 28px; flex-wrap: wrap }
.case-stat-num {
  font-family: var(--display); font-size: clamp(2.2rem, 4vw, 3rem);
  color: var(--c); line-height: 1; display: block;
}
.case-stat-unit { font-size: .5em; color: var(--dim) }
.case-stat-label {
  font-family: var(--mono); font-size: .68rem; letter-spacing: 1px;
  color: var(--muted); line-height: 1.5; display: block; margin-top: 8px;
}

.contact-cta {
  margin-top: 24px; padding: 48px;
  background: var(--surface); border: 1px solid var(--border);
  border-radius: 20px;
  display: flex; align-items: center;
  justify-content: space-between; gap: 32px; flex-wrap: wrap;
}
.cta-title {
  font-family: var(--display); font-size: clamp(1.8rem, 3.5vw, 2.6rem);
  letter-spacing: 2px; margin-bottom: 8px;
}
.cta-sub { color: var(--muted); font-size: var(--text-base); line-height: 1.7; max-width: 48ch }
</style>
